// import { mount } from '@src/helpers/test'
// import { useCookie } from '@src/vue-use-kit'

afterEach(() => {
  jest.clearAllMocks()
})

describe('useCookie', () => {
  it('should do something', () => {
    // Add test here
  })
})
