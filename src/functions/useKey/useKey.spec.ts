// import { mount } from '@src/helpers/test'
// import { useKey } from '@src/vue-use-kit'

afterEach(() => {
  jest.clearAllMocks()
})

describe('useKey', () => {
  it('should do something', () => {
    // Add test here
  })
})
