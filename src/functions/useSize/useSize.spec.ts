// import { mount } from '@src/helpers/test'
// import { useSize } from '@src/vue-use-kit'

afterEach(() => {
  jest.clearAllMocks()
})

describe('useSize', () => {
  it('should do something', () => {
    // Add test here
  })
})
