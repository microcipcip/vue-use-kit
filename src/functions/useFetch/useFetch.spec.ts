// import { mount } from '@src/helpers/test'
// import { useFetch } from '@src/vue-use-kit'

afterEach(() => {
  jest.clearAllMocks()
})

describe('useFetch', () => {
  it('should do something', () => {
    // Add test here
  })
})
